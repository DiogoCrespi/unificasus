<Window x:Class="UnificaSUS.WPF.RelatoriosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Relatórios de Procedimentos" 
        Height="650" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="LightGray">
    <Window.Resources>
        <!-- Estilo para arredondar TextBox (campos de entrada) -->
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Focusable="False"
                                        HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                                        VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#A0A0A0"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3,2"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <!-- Estilo para arredondar ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5">
                                <Border.Effect>
                                    <DropShadowEffect Color="Gray" 
                                                    Direction="315" 
                                                    ShadowDepth="2" 
                                                    Opacity="0.3" 
                                                    BlurRadius="4"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton Grid.ColumnSpan="2"
                                                Name="ToggleButton"
                                                Focusable="False"
                                                IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                                ClickMode="Press"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Cursor="Hand">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter Grid.Column="0"
                                                    Name="ContentSite"
                                                    IsHitTestVisible="False"
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                    Margin="5,2,2,2"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left"/>
                                    <Path Grid.Column="1"
                                        Fill="DarkGray" 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center" 
                                        Data="M 0 0 L 4 4 L 8 0 Z"
                                        IsHitTestVisible="False"/>
                                </Grid>
                            </Border>
                            <ContentPresenter Name="ContentSite2"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="5,2,25,2"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"
                                            Visibility="Hidden"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                   Style="{x:Null}"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Margin="5,2,25,2"
                                   Focusable="True"
                                   Background="Transparent"
                                   Visibility="Hidden"
                                   IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True" 
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                      Background="White"
                                      BorderThickness="1"
                                      BorderBrush="Gray"
                                      CornerRadius="5"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Gray" 
                                                        Direction="315" 
                                                        ShadowDepth="3" 
                                                        Opacity="0.4" 
                                                        BlurRadius="6"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="4,6,4,6" 
                                                SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Gray" 
                                                        Direction="315" 
                                                        ShadowDepth="3" 
                                                        Opacity="0.4" 
                                                        BlurRadius="5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="Gray"/>
                                <Setter TargetName="MainBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Gray" 
                                                        Direction="315" 
                                                        ShadowDepth="1" 
                                                        Opacity="0.1" 
                                                        BlurRadius="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Estilo para arredondar ComboBoxItem -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="0"
                                CornerRadius="3"
                                Padding="5,2"
                                Margin="2">
                            <ContentPresenter HorizontalAlignment="Left" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0E8FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Estilo para arredondar botões com sombras -->
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Cursor="Hand">
                            <Border.Effect>
                                <DropShadowEffect Color="Gray" 
                                                Direction="315" 
                                                ShadowDepth="2" 
                                                Opacity="0.3" 
                                                BlurRadius="4"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding Content}"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Gray" 
                                                        Direction="315" 
                                                        ShadowDepth="3" 
                                                        Opacity="0.4" 
                                                        BlurRadius="5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Gray" 
                                                        Direction="315" 
                                                        ShadowDepth="1" 
                                                        Opacity="0.2" 
                                                        BlurRadius="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Gray" 
                                                        Direction="315" 
                                                        ShadowDepth="1" 
                                                        Opacity="0.1" 
                                                        BlurRadius="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,2"/>
        </Style>
        
        <!-- Estilo para arredondar GroupBox -->
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="Gray" 
                                                Direction="315" 
                                                ShadowDepth="1" 
                                                Opacity="0.2" 
                                                BlurRadius="3"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <ContentPresenter Grid.Row="0" 
                                                ContentSource="Header"
                                                RecognizesAccessKey="True"
                                                Margin="5,0,0,5"/>
                                <ContentPresenter Grid.Row="1" 
                                                ContentSource="Content"
                                                Margin="0"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Estilo para arredondar ListBox -->
        <Style TargetType="ListBox">
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Estilo para ListBoxItem -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0"
                                CornerRadius="2"
                                Padding="3,2"
                                Margin="1">
                            <ContentPresenter HorizontalAlignment="Left" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0E8FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Seção Superior: Seleção de Filtro -->
        <GroupBox Grid.Row="0" Header="Selecionar por:" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Radio Buttons para tipo de filtro -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Margin="0,0,10,10" ToolTip="Selecione o tipo de filtro para buscar procedimentos">
                    <RadioButton x:Name="RbGrupo" Content="Grupo" IsChecked="True" Margin="0,2" Checked="TipoFiltro_Changed" ToolTip="Filtrar por grupo de procedimentos"/>
                    <RadioButton x:Name="RbSubGrupo" Content="Sub-grupo" Margin="0,2" Checked="TipoFiltro_Changed" ToolTip="Filtrar por sub-grupo de procedimentos"/>
                    <RadioButton x:Name="RbFormaOrganizacao" Content="Forma de organização" Margin="0,2" Checked="TipoFiltro_Changed" ToolTip="Filtrar por forma de organização"/>
                    <RadioButton x:Name="RbTipoLeito" Content="Tipo de Leito" Margin="0,2" Checked="TipoFiltro_Changed" ToolTip="Filtrar por tipo de leito"/>
                    <RadioButton x:Name="RbInstrumentoRegistro" Content="Instrumento de Registro" Margin="0,2" Checked="TipoFiltro_Changed" ToolTip="Filtrar por instrumento de registro"/>
                    <RadioButton x:Name="RbProcedimento" Content="Procedimento" Margin="0,2" Checked="TipoFiltro_Changed" ToolTip="Filtrar por procedimento específico"/>
                </StackPanel>

                <!-- ComboBox editável com lista de todos os itens -->
                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,10,0">
                    <ComboBox x:Name="ComboBoxItens" 
                             Width="300" 
                             Margin="0,0,5,0"
                             IsEditable="True"
                             TextBoxBase.TextChanged="ComboBoxItens_TextChanged"
                             SelectionChanged="ComboBoxItens_SelectionChanged"
                             KeyDown="ComboBoxItens_KeyDown"
                             ToolTip="Digite ou selecione um item da lista"/>
                    <Button Content="→" Width="30" Height="25" Click="BtnAdicionar_Click" Background="LightGreen" Foreground="White" FontWeight="Bold" ToolTip="Adicionar item à lista de impressão"/>
                </StackPanel>

                <!-- ListBox de itens selecionados -->
                <GroupBox Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Header="Imprimir a seleção abaixo:" Padding="5">
                    <ListBox x:Name="ListBoxItensSelecionados" MinHeight="100" MaxHeight="150" ToolTip="Lista de itens selecionados para impressão"/>
                </GroupBox>
            </Grid>
        </GroupBox>

        <!-- Seção Intermediária: Ações e Configurações -->
        <GroupBox Grid.Row="1" Header="Configurações do Relatório" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Título do relatório -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Título do relatório:" VerticalAlignment="Center" Margin="0,0,5,0" ToolTip="Campo para definir o título do relatório"/>
                    <TextBox x:Name="TxtTituloRelatorio" Width="300" Text="Relatório de Procedimentos" ToolTip="Título que aparecerá no relatório"/>
                </StackPanel>

                <!-- Checkbox Não Imprimir SP zerado -->
                <CheckBox x:Name="ChkNaoImprimirSPZerado" 
                         Grid.Row="1" Grid.Column="0"
                         Content="Não Imprimir procedimentos com SP zerado" 
                         Margin="0,0,0,10"
                         ToolTip="Se marcado, exclui procedimentos com Valor S.P. = 0 do relatório"/>

                <!-- Botões Limpar e Imprimir -->
                <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right">
                    <Button x:Name="BtnLimpar" Content="Limpar" Width="100" Height="30" Margin="0,0,0,5" Click="BtnLimpar_Click" ToolTip="Remove todos os itens da lista"/>
                    <Button x:Name="BtnImprimir" Content="Imprimir" Width="100" Height="30" Click="BtnImprimir_Click" Background="LightBlue" ToolTip="Gera e imprime o relatório"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Seção Inferior: Modelo e Ordenação -->
        <GroupBox Grid.Row="2" Header="Opções de Relatório" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Modelo do relatório -->
                <GroupBox Grid.Column="0" Header="Modelo do relatório:" Padding="5" Margin="0,0,5,0">
                    <RadioButton x:Name="RbModeloCodigoNomeValorSP" Content="Código, nome e valor do SP" IsChecked="True" ToolTip="Exibe código, nome e valor do Serviço Profissional"/>
                </GroupBox>

                <!-- Ordenar por -->
                <GroupBox Grid.Column="1" Header="Ordenar por:" Padding="5" Margin="5,0,0,0">
                    <StackPanel>
                        <RadioButton x:Name="RbOrdenarPorCodigo" Content="Código Procedimento" IsChecked="True" Margin="0,2" ToolTip="Ordena por código do procedimento"/>
                        <RadioButton x:Name="RbOrdenarPorNome" Content="Nome" Margin="0,2" ToolTip="Ordena por nome do procedimento"/>
                        <RadioButton x:Name="RbOrdenarPorValorSP" Content="Valor do SP" Margin="0,2" ToolTip="Ordena por valor do Serviço Profissional (decrescente)"/>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" VerticalAlignment="Bottom" ToolTip="Barra de status do sistema">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="Pronto" ToolTip="Status atual do sistema"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>

